# AbsoluteTouchEx 仓库分析报告

> 分析日期：2026-07-31
> 仓库路径：`D:\孩子\github\AbsoluteTouchEx`
> 上游项目：[apsun/AbsoluteTouchEx](https://github.com/apsun/AbsoluteTouchEx)
> 分析方式：README 通读 + 4 个代码探索子代理并行深度审查（atdll / atloader / attest / 构建系统）

---

## 一、项目概述

**AbsoluteTouchEx** 是一款 Windows 工具，将精密触控板（Precision Touchpad）当作触摸屏使用，实现"绝对光标定位"——手指在触控板上的位置直接对应屏幕坐标，而非传统触控板的相对位移。

它是 [AbsoluteTouch](https://github.com/apsun/AbsoluteTouch) 的下一代，解决了原版的两大缺陷：
1. 仅支持 Synaptics 触控板 → 本版兼容几乎所有 Windows 精密触控板；
2. 在慢速机器上严重卡顿 → 本版性能提升数个数量级。

**核心原理（关键设计）**：通过注入目标进程并 Hook Windows API，将 HID 触控板事件在进程内翻译为绝对坐标的鼠标事件，而非像原版那样在独立进程内全局设置光标位置。这带来了极致性能，但也意味着它"更像 hack"，可能触发游戏反作弊系统（README 已明确警告）。

---

## 二、系统架构与技术原理

整体由三个子项目构成，运行期依赖、构建期独立：

| 子项目 | 类型 | 职责 |
|--------|------|------|
| `atdll` | DLL（注入体） | 核心逻辑：Hook + HID 解析 + 坐标映射 |
| `atloader` | EXE（加载器） | 以挂起态创建目标进程并注入 atdll.dll |
| `attest` | EXE（验证工具） | 被动监听绝对坐标鼠标输入，验证功能是否生效 |

### 2.1 注入机制
加载器使用 Detours 的 `DetourCreateProcessWithDllExA`（atloader.cpp:51），以挂起态创建目标进程 → 写入 Detours 引导代码 → 引导代码在目标 `main/WinMain` 之前调用 `LoadLibraryA("atdll.dll")` → 恢复主线程。相比 `CreateRemoteThread + LoadLibrary` 或 `SetWindowsHookEx`，创建期注入对受保护/自校验程序侵入性更低、时机更早。

### 2.2 Hook 链与事件桥接（atdll 设计亮点）
DLL 在 `DllMain`（atdll.cpp:1189）的 `DLL_PROCESS_ATTACH` 中自初始化：`DetourRestoreAfterWith()` → `AT_StartDebugMode()` → `AT_Initialize()`，并通过 Detours 事务 Hook 以下函数：

- `RegisterRawInputDevices`（atdll.cpp:934）：拦截鼠标注册窗口，注入触控板设备与热键；
- `GetRawInputData`（atdll.cpp:813）：对 `MAGIC_HANDLE(0)` 返回合成的绝对鼠标 `RAWINPUT`；
- `CreateWindowExW`（atdll.cpp:1040）：替换目标窗口的 WndProc；
- `Get/SetWindowLongPtrW`（atdll.cpp:978/991）：隐藏假冒 WndProc。

**精巧桥接**：`AT_TouchpadToScreen`（atdll.cpp:433）将 HID 物理坐标按校准区域线性映射到 0–65535（含夹取、`+1` 修正），写入 `RAWINPUT` 并置 `MOUSE_MOVE_ABSOLUTE`、把 `lParam` 改为 `MAGIC_HANDLE`，交由真实 WndProc 经 Hook 取回合成输入——避免了递归调用，是整套方案最巧妙之处。

### 2.3 HID 解析与校准
- `AT_GetContacts`（atdll.cpp:542）使用 `HidP_GetCaps / GetButtonCaps / GetValueCaps / GetUsages / GetScaledUsageValue` 解析触点（tip / contactID / X / Y 物理值）；
- `AT_GetPrimaryContact`（atdll.cpp:624）选取主触点；
- 校准：<kbd>SHIFT+F7</kbd> 进入校准（累积触点包围盒为 `touchAreaOverride`），<kbd>SHIFT+F9</kbd> 写 `atcalibration.conf`（KEY VALUE 纯文本），<kbd>SHIFT+F8</kbd> 读回，<kbd>SHIFT+F6</kbd> 为绝对模式总开关。

---

## 三、构建系统分析

- **解决方案** `AbsoluteTouchEx.sln`（VS2019 / v142），含三项目，sln 无 `ProjectDependencies`，构建顺序依赖运行期而非编译期。
- **平台工具集** `v142`；Windows SDK 仅写 `10.0`（需本机安装）；运行库默认 `/MD`、`/MDd`（需装 VC++2019 运行库）。
- **四配置** Debug/Release × Win32/x64。
- **Detours**：预编译 `detours32.lib` / `detours64.lib` 直接置于 `atdll\`、`atloader\` 目录（各一份），`detours.h` 重复放置（内容一致）。
- **HID**：仅 atdll 链接 `hid.lib`（来自 Windows SDK/WDK），`hidpi.h` 经 SDK 默认路径供给。

### ⚠️ 致命构建缺陷（必须修复）
**`atdll.vcxproj` 与 `atloader.vcxproj` 同时将 `detours32.lib` 与 `detours64.lib` 无条件列入 `<AdditionalDependencies>`，未按 `$(Platform)` 区分。** 链接时异架构 `.lib` 同链会触发 **LNK1112（模块机器类型冲突）**，导致 **任意配置均无法链接成功**。这是仓库当前最严重的问题，修正方法：用 `Condition="'$(Platform)'=='Win32'"` / `'$(Platform)'=='x64'` 仅链入对应库。

---

## 四、代码审查（按审查总则分级）

> 缺陷等级：🔴 致命 / 🟠 严重 / 🟡 一般 / 🟢 优化建议

### 4.1 构建与配置（致命）
- 🔴 **LNK1112 链接冲突**：如上所述，双架构 lib 同链。整改：按平台条件链接。

### 4.2 安全与合规风险（严重）
- 🟠 **反作弊触发风险**：注入型 DLL 可能触发游戏反作弊（README 已声明免责），属设计固有风险，使用者需自行承担。
- 🟠 **DllMain 高危操作**（atdll.cpp:1189）：在 `DLL_PROCESS_ATTACH` 内执行 `AllocConsole` + Detours Attach，处于加载器锁（loader lock）上下文，存在死锁与破坏目标 GUI 的隐患。建议将重初始化移出 DllMain，或仅做最小注册、延迟到首次调用。
- 🟠 **全局非线程安全**（atdll.cpp `g_devices`）：`unordered_map` 在多窗口/多线程场景下无锁，存在竞态。
- 🟠 **真实鼠标被吞**（atdll）：启用绝对模式后拦截所有 `RIM_TYPEMOUSE`/`WM_MOUSEMOVE`，真实鼠标失效（代码注释已承认），属功能副作用，需文档明示。

### 4.3 异常与容错（一般/严重）
- 🟠 **atloader 异常未捕获**：`win32_error`（私有继承 `std::exception`）在 `main` 无 `try/catch`，抛异常将 `std::terminate` 直接崩溃；且进程创建失败（atloader.cpp:65）仍 `return 0`，误导脚本判断。
- 🟡 **atloader 句柄泄漏**：未 `CloseHandle(procInfo.hProcess/hThread)`，也未 `WaitFor*` 确认 DLL 实际加载。
- 🟡 **attest 错误不可见**：`win32_error` 未重写 `what()`，`main` 无捕获，排错困难。

### 4.4 逻辑与边界（一般）
- 🟡 **atloader 无位数检测**：运行时由用户自行选 x86/x64 加载器，用错位数注入必败且无提示；建议增加 PE 头解析自动判定。
- 🟡 **校准配置解析脆弱**：`fscanf("%80s%ld")` 对 `touchAreaOverride` 无完整校验，畸形文件可能解析失败。
- 🟡 **attest 注释与行为不一致**：注释称创建 invisible window（attest.cpp:130），实际用 `WS_OVERLAPPEDWINDOW`（attest.cpp:137）会弹出可见窗口，且工程定义为 `_CONSOLE`，三者不一致。

### 4.5 编码规范与可维护性（一般/优化）
- 🟡 **硬编码与 ANSI 限制**：atloader 使用 `GetModuleFileNameA`/`'\\'`/`MAX_PATH`，非 ASCII 路径与含空格路径脆弱，建议 `W` 版本 + `std::filesystem`。
- 🟢 **Debug 日志残留**：`DEBUG_MODE=1` 时 `atdebug.log` 在 Release 仍写盘且路径硬编码，应受编译开关或环境变量控制。
- 🟢 **detours.h 重复**：两目录各存一份，建议提为公共 include 避免漂移。

---

## 五、技术亮点总结

1. **创建期注入**：Detours `DetourCreateProcessWithDllExA` 比远程线程更稳健，适合游戏场景。
2. **MAGIC_HANDLE 桥接**：合成绝对鼠标输入经真实 WndProc 取回，巧妙规避递归与重入。
3. **HID 精确解析**：完整使用 HIDP 函数族解析多点触控报告，物理坐标映射严谨。
4. **解耦验证工具**：attest 不依赖被测 DLL，纯被动观测，定位清晰。

---

## 六、结论与建议

该仓库是一个**思路精巧、原理清晰的 Windows 触控板绝对定位工具**，核心 Hook 与坐标映射设计值得肯定。但当前存在**一个致命构建缺陷（Detours 双 lib 同链导致无法链接）**和多处健壮性、安全边界问题。

**优先整改清单**：
1. 🔴 按平台条件区分链接 `detours32.lib` / `detours64.lib`（修复 LNK1112）；
2. 🟠 atloader 增加异常捕获与正确的退出码、关闭句柄、目标位数检测；
3. 🟠 atdll 将 DllMain 重活延迟，消除加载器锁风险；
4. 🟡 attest 修正窗口样式与注释一致性、补全错误处理；
5. 🟢 统一使用宽字符 API 与 `std::filesystem`，消除路径脆弱性。

如需，我可直接对上述第 1 项（致命构建缺陷）进行修复并验证编译配置。
